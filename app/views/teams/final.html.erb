<div class="container text-center w-75">
  <br>
  <div class="green"><h1>League: <%= @team.league.name %></h1></div>
  <div class="green"><h2>Your team: <%= @team.name %></h2></div>
  <br>
  <div class="green"><h3>Congratulations! You have finalised your team with 8 champions!</h3></div>

  <table class="table">
    <thead>
      <tr>
        <th scope="col">Player</th>
        <th scope="col">Your winning bid</th>
      </tr>
    </thead>
    <tbody>
    <% @current_user_secured_selections.each do |selection| %></ul>
      <tr>
        <td><%=Player.find(selection[:player_id]).first_name%> <%=Player.find(selection[:player_id]).last_name%></td>
        <td><%=selection.price%> m€ </td>
      </tr>
    <% end %>
    <tr>
      <td></td>
      <td></td>
    </tr>

    </tbody>
  </table>

  <br>

  <h3>Here is a recap of the bids by your opponents won during the auction rounds!</h3>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">Player</th>
        <th scope="col">Winning bid</th>
        <th scope="col">Winning team</th>
      </tr>
    </thead>
    <tbody>
    <% @opponents_secured_selections.each do |selection| %></ul>
      <tr>
        <td><%=Player.find(selection[:player_id]).first_name%> <%=Player.find(selection[:player_id]).last_name%></td>
        <td><%=selection.price%> m€ </td>
        <td><%=Team.find(selection[:team_id]).name%> </td>
      </tr>
    <% end %>
    </tbody>
  </table>

  <% if @league.all_finalized? %>

    <h2>All teams of the leagues are now finalized!</h2>
    <h2>Let's get ready to ruuuuuuuuuumble</h2>
    <%= link_to "Let's start the league!", league_path(@league), class: "btn btn-success" %>


  <% else %>

    <h3>We're still waiting for your fellow opponents to finalize their teams...</h2>

    <%= link_to "Refresh", final_path(@league, @team), class: "btn btn-success" %>


  <% end %>


</div>
