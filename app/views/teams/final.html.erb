<div class="container text-center w-75">

  <div class="chatroom-title">
    <h1>Your team is ready</h1>
  </div>
  <div class="back-shadow">
    <table class="table">
      <h2><%= @team.name %></h2>
      <thead>
        <tr>
          <th scope="col">Player</th>
          <th scope="col">Your winning bid</th>
        </tr>
      </thead>
      <tbody>
      <% @current_user_secured_selections.each do |selection| %></ul>
        <tr>
          <td><%=Player.find(selection[:player_id]).first_name%> <%=Player.find(selection[:player_id]).last_name%></td>
          <td><%=selection.price%> m€ </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>

   <% if @league.all_finalized? %>
      <div class="center mt-2"><%= link_to "See your team", [@league, @team], class: "bouton-plain-share" %></div>
    <% else %>
      <div class="back-shadow mt-4">
        <h3>Bidding rounds still pending ...</h3>
      </div>
      <div class="center"><%= link_to "Refresh", final_path(@league, @team), class: "bouton-plain-share" %></div>
    <% end %>
  </div>
</div>
