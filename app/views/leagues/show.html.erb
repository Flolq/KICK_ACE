<div class="container">
<h1><%= @league.name %></h1>

<div class="ranking-card-container">
  <div class="ranking-laurier"></div>
  <div class="ranking-card">
    <div class="ranking-card-title">Ranking</div>
    <ul>
      <% @teams.each do |team| %>
        <li><%= link_to "#{team.name} - #{team.user.nickname} - #{team.points}" , [@league, team] %></li>
      <% end %>
    </ul>
  </div>
</div>

<%= link_to @team.name, [@league, @team] %>

<h2>Your Team</h2>
<%= @team.name %>
<% i = 1 %>
<ul><% @team.selections.each do |selection| %></ul>
    <li><%= i %>. <%= selection.player.first_name %> - Price : <%= selection.price %>M€</li>
    <% i +=1 %>
<% end %>

<%= link_to "homepage", user_path(current_user) %>
</div>
